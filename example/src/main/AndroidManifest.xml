<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.samsandberg.stravaauthenticatorexample">

    <application
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <!-- TODO: Figure out why ExampleAuthActivity has no label, even though it's set -->
        <activity
            android:name="com.samsandberg.stravaauthenticatorexample.ExampleAuthActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.samsandberg.stravaauthenticatorexample.ExampleMainActivity"
            android:label="@string/app_name">
        </activity>
    </application>

</manifest>
